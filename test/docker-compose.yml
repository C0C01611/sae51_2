version: '3'
services:
  logstash:
    build: .
    ports:
      - "5044:5044"
    depends_on:
      - elasticsearch
    volumes:
      - ./logs.json:/app/logs.json

  elasticsearch:
    image: elasticsearch:8.7.0
    ports:
      - "9200:9200"